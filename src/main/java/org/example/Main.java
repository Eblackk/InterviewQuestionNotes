package org.example;

class Solution_BreakDown {
    public Integer totalFruits(int[] fruits) {
        //An Array is a container object that holds a fixed number of values of a single type
        //Each item in an array is called an "ELEMENT"
        // One array can have more than one "SUB-ARRAY" withIn
        // Array Ex. [1,2,3,4]
        //SUB-ARRAY EX. [1,2], [3,4], [1,2,3], and [2,3,4]


        //Set<> is a collection of elements/objects. Which contains no duplicates
        //Set<> is an interface that extends "Collection-InterFace"
        //Set<> does NOT support indexes or positions of its elements

        // HashSet<> is the implementation of Set Interface
        // HashSet<> internally uses HashMap for its implementation

        return null;
    }

}